# Figure 3D Representative-Sample Axis Validation

The rightmost x-axis label `156` is correct.

Figure 3D displays 156 representative paired samples selected deterministically from the Stage18 paired gut-omics data:

- 52 non-IBD/HC samples;
- 52 UC samples;
- 52 CD samples.

The microbiome and metabolome heatmaps use the same ordered 156 sample columns. The sample annotations and matrix columns are aligned exactly. The plotted x-axis positions are zero-based indices `0`, `51`, `103`, and `155`, displayed as one-based labels `1`, `52`, `104`, and `156`.

This is a representative heatmap, not the full 388-sample modelling data set. The plotted feature blocks contain 470 microbiome rows and 1,000 metabolome rows.

Source audit:

`Fingerprint Tables/Charts/Figure3/panel_B_real_data_density_heatmap_20260618_182915`
